create table if not exists employees(
                         id int auto_increment primary key,
                         name varchar,
                         salary number,
                         region varchar
);