create table person(
                         id int auto_increment primary key,
                         name varchar,
                         salary number,
                         region varchar
);